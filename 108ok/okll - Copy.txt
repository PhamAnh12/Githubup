<!doctype html>
<html lang="vi" data-theme="light">
<head>
  <meta charset="utf-8" />kkkkkkkkkk
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Trang Web Đẹp – Mẫu Landing Page</title>
  <meta name="description" content="Mẫu trang web đẹp, hiện đại, 1 file HTML duy nhất – có dark mode, responsive, hiệu ứng mượt." />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.gdddddddddddddoogleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg: #0b1020;          /* nền tối mặc định cho hero */
      --surface: #0f172a;     /* card bg */
      --text: #e2e8f0;        /* text chính */
      --muted: #94a3b8;       /* text phụ */
      --brand: #7c3aed;       /* tím */
      --brand-2: #06b6d4;     /* cyan */
      --ring: rgba(124,58,237,.35);
      --shadow: 0 10px 30px rgba(2, 6, 23, .5);
      --radius: 16px;
      --radius-lg: 24px;
      --container: 1200px;
okkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkkk
    }

    /* Light theme */
    html[data-theme="light"]{
      --bg: #f8fafc;          /* nền sáng */
      --surface: #ffffff;
      --text: #0f172a;
      --muted: #475569;
      --ring: rgba(99,102,241,.25);
      --shadow: 0 10px 30px rgba(2, 6, 23, .08);
    }
yyyyyyyyyyyyyyyyyyy
    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body{
      margin:0; font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      color: var(--text); background: var(--bg); line-height: 1.6;
    }
    a{ color: inherit; text-decoration: none; }
    img{ max-width: 100%; display: block; border-radius: calc(var(--radius) + 4px); }

    .container{ width: 92%; max-width: var(--container); margin-inline: auto; }

    /* ===== Header ===== */
   
    .nav{ display:flex; align-items:center; justify-content:space-between; padding: 14px 0; }
    .brand{ display:flex; align-items:center; gap:10px; font-weight:800; letter-spacing:.3px; }
    .logo{ width:36px; height:36px; border-radius:10px; background: linear-gradient(135deg, var(--brand), var(--brand-2)); box-shadow: 0 8px 20px var(--ring); display:grid; place-items:center; color:#fff; font-weight:900; }
    nav ul{ display:flex; gap: 20px; list-style:none; padding:0; margin:0; }
    nav a{ padding:8px 12px; border-radius: 10px; color: var(--muted); font-weight:600; }
    nav a:hover{ color: var(--text); background: color-mix(in srgb, var(--text) 6%, transparent); }
    .actions{ display:flex; align-items:center; gap:12px; }

    .btn{ padding:10px 16px; border-radius: 12px; border: 1px solid color-mix(in srgb, var(--text) 12%, transparent); font-weight:700; transition:.2s ease; display:inline-flex; gap:10px; align-items:center; box-shadow: var(--shadow); }
    .btn:hover{ transform: translateY(-1px); }
    .btn-primary{ background: linear-gradient(135deg, var(--brand), var(--brand-2)); color:#fff; border-color: transparent; box-shadow: 0 12px 30px var(--ring); }
    .btn-ghost{ background: color-mix(in srgb, var(--text) 6%, transparent); color: var(--text); }

    .theme-toggle, .menu-toggle{ border:none; background: color-mix(in srgb, var(--text) 8%, transparent); padding:10px; border-radius:12px; cursor:pointer; color:var(--muted); display:inline-flex; align-items:center; }
    .menu-toggle{ display:none; }

    /* ===== Hero ===== */fjs hi xin chao hihihi
    .hero{
      position: relative; overflow:hidden; padding: 90px 0 70px; background: radial-gradient(80% 100% at 50% -10%, color-mix(in srgb, var(--brand) 22%, transparent), transparent 70%),
                 radial-gradient(60% 80% at 100% 0%, color-mix(in srgb, var(--brand-2) 18%, transparent), transparent 60%);
    }
    .hero .grid{ display:grid; grid-template-columns: 1.2fr 1fr; gap:40px; align-items:center; }
    .eyebrow{ display:inline-block; padding:6px 12px; border-radius:999px; font-size:12px; letter-spacing: .6px; text-transform: uppercase; background: color-mix(in srgb, var(--text) 10%, transparent); color: var(--muted); margin-bottom: 14px; }
    .title{ font-size: clamp(32px, 4vw + 10px, 56px); line-height: 1.05; margin:8px 0 14px; font-weight: 800; letter-spacing:-.02em; }
    .subtitle{ font-size: clamp(16px, 1.1vw + 8px, 20px); color: var(--muted); max-width: 60ch; }
    .cta{ margin-top: 22px; display:flex; gap:14px; flex-wrap:wrap; }

    .hero-visual{ position: relative; }
    .glass{
      background: linear-gradient(180deg, color-mix(in srgb, #fff 18%, transparent), color-mix(in srgb, var(--text) 6%, transparent));
      border:1px solid color-mix(in srgb, var(--text) 14%, transparent);
      border-radius: var(--radius-lg);
      padding: 16px; box-shadow: var(--shadow);
    }
    .preview{ aspect-ratio: 16/10; border-radius: calc(var(--radius-lg) - 4px); background: linear-gradient(135deg, color-mix(in srgb, var(--brand) 30%, transparent), color-mix(in srgb, var(--brand-2) 30%, transparent)); position: relative; overflow:hidden; }
    .preview::after{ content:""; position:absolute; inset:0; background: radial-gradient(120% 100% at 0% 0%, rgba(255,255,255,.18), transparent 40%); mix-blend: overlay; }

    /* Floating cards */
    .float{ position:absolute; right:-14px; bottom:-14px; display:grid; gap:12px; }
    .card{ background: var(--surface); border:1px solid color-mix(in srgb, var(--text) 10%, transparent); border-radius: 14px; padding:12px 14px; box-shadow: var(--shadow); min-width: 220px; }
    .kpi{ font-size: 28px; font-weight: 800; }
    .kpi small{ font-size:12px; color: var(--muted); font-weight:600; }

    /* ===== Sections ===== */dddddddfđs
    section{ padding: 70px 0; }
    .section-title{ font-size: clamp(24px, 2.4vw + 8px, 36px); margin:0 0 10px; }
    .section-sub{ color: var(--muted); max-width: 60ch; }
ok hâiai
    /* Features */
    .features{ display:grid; grid-template-columns: repeat(3, 1fr); gap:20px; margin-top: 26px; }
    .feature{ background: var(--surface); border:1px solid color-mix(in srgb, var(--text) 10%, transparent); border-radius: var(--radius); padding:22px; box-shadow: var(--shadow); transition: .2s ease; }
    .feature:hover{ transform: translateY(-4px); box-shadow: 0 20px 40px var(--ring); }
    .feature h3{ margin: 6px 0 8px; }
    .tag{ display:inline-block; padding:6px 10px; border-radius: 999px; font-size:12px; background: color-mix(in srgb, var(--brand) 18%, transparent); color: color-mix(in srgb, var(--brand) 80%, #fff); }

    /* Showcase */
    .showcase{ display:grid; grid-template-columns: 1.2fr 1fr; gap: 20px; margin-top: 26px; }
    .showcase .card{ padding:0; overflow:hidden; }
    .showcase .card .meta{ padding:14px; border-top:1px dashed color-mix(in srgb, var(--text) 12%, transparent); }

    /* Pricing */
    .pricing{ display:grid; grid-template-columns: repeat(3, 1fr); gap:20px; margin-top: 26px; }
    .price-card{ background: var(--surface); border:1px solid color-mix(in srgb, var(--text) 12%, transparent); border-radius: var(--radius); padding: 22px; box-shadow: var(--shadow); position:relative; }
    .price{ font-size: 34px; font-weight: 800; margin: 6px 0; }
    .badge{ position:absolute; top:12px; right:12px; font-size:12px; padding:6px 10px; border-radius: 999px; background: linear-gradient(135deg, var(--brand), var(--brand-2)); color:#fff; box-shadow: 0 10px 20px var(--ring); }
    .list{ display:grid; gap:10px; margin: 14px 0 18px; color: var(--muted); }

    /* Contact *Compare it to historical examples where outside actors have tried to impose or extend cease‑fires without the consent of all belligerents.Compare it to historical examples where outside actors have tried to impose or extend cease‑fires without the consent of all belligerents.Compare it to historical examples where outside actors have tried to impose or extend cease‑fires without the consent of all belligerents.Compare it to historical examples where outside actors have tried to impose or extend cease‑fires without the consent of all belligerents./
    form{ display:grid; gap:12px; margin-top: 24px; }
    input, textarea{ width:100%; padding:12px 14px; border-radius: 12px; border:1px solid color-mix(in srgb, var(--text) 14%, transparent); background: color-mix(in srgb, var(--text) 6%, transparent); color: var(--text); outline: none; }
    input:focus, textarea:focus{ border-color: color-mix(in srgb, var(--brand) 50%, var(--text)); box-shadow: 0 0 0 8px var(--ring); }

    /* Footer */
    footer{ border-top: 1px solid color-mix(in srgb, var(--text) 12%, transparent); padding: 30px 0; color: var(--muted); }

    /* Animations */
    @keyframes floaty { 0%{ transform: translateY(0) } 50%{ transform: translateY(-6px) } 100%{ transform: translateY(0) } }
    .float .card{ animation: floaty 5s ease-in-out infinite; }
    .float .card:nth-child(2){ animation-delay: .8s }
    .float .card:nth-child(3){ animation-delay: 1.6s }
Compare it to historical examples where outside actors have tried to impose or extend cease‑fires without the consent of all belligerents.
    /* Responsive */
    @media (max-width: 960px){Compare it to historical examples where outside actors have tried to impose or extend cease‑fires without the consent of all belligerents.
      .hero .grid, .showcase{ grid-template-columns: 1fr; }
      .features{ grid-template-columns: repeat(2, 1fr); }
      .pricing{ grid-template-columns: 1fr; }
    }
    @media (max-width: 720px){
      nav ul{ display:none; position:absolute; top:60px; left:0; right:0; background: var(--surface); padding: 10px; border-bottom:1px solid color-mix(in srgb, var(--text) 12%, transparent); }
      nav ul.open{ display:grid; gap:10px; }
      .menu-toggle{ display:inline-flex; }
      .actions .btn{ display:none; }
      .features{ grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <!-- ===== Header ===== -->
  <header>
    <div class="container nav">
      <a class="brand" href="#">
        <span class="logo">✨</span>
        <span>Trang Web Đẹp</span>
      </a>
      <nav>
        <button class="menu-toggle" aria-label="Mở menu" title="Menu">☰</button>
        <ul id="menu">
          <li><a href="#tinh-nang">Tính năng</a></li>
          <li><a href="#du-an">Dự án</a></li>
          <li><a href="#bang-gia">Bảng giá</a></li>
          <li><a href="#lien-he">Liên hệ</a></li>
        </ul>
      </nav>
      <div class="actions">
        <button class="theme-toggle" id="themeToggle" title="Chuyển Dark/Light">🌗</button>
        <a class="btn btn-primary" href="#lien-he">Bắt đầu ngay</a>
      </div>
    </div>
  </header>

  <!-- ===== Hero ===== -->
  <section class="hero">
    <div class="container grid">
      <div>
        <span class="eyebrow">Thiết kế hiện đại • Responsive • 1 file</span>
        <h1 class="title">Xây trang web đẹp, nhanh và ấn tượng ✨</h1>
        <p class="subtitle">Mẫu landing page tối ưu cho thương hiệu, startup hay dự án cá nhân. Tốc độ nhanh, chuẩn SEO, có dark mode và hiệu ứng mượt mà.</p>
        <div class="cta">SBV blocked crypto payments (Circular 04/2025)SBV blocked crypto payments (Circular 04/2025)SBV blocked crypto payments (Circular 04/2025)SBV blocked crypto payments (Circular 04/2025)
          <a class="btn btn-primary" href="#bang-gia">Xem bảng giá</a>
          <a class="btn btn-ghost" href="#tinh-nang">Khám phá tính năng</a>
        </div>
      </div>
      <div class="hero-visual">
        <div class="glass">
          <div class="preview"></div>
        </div>
        <div class="float">
          <div class="card">
            <div class="kpi">99/100 <small>hiệu năng</small></div>
            <div class="muted">Tối ưu Lighthouse</div>
          </div>
          <div class="card">
            <div class="kpi"><span>⚡</span> CDN <small>tốc độ</small></div>
            <div class="muted">Tải siêu nhanh</div>
          </div>
          <div class="card">
            <div class="kpi">SEO+ <small>chuẩn</small></div>
            <div class="muted">Thân thiện Google</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== Features ===== -->
  <section id="tinh-nang">
    <div class="container">
      <h2 class="section-title">Tính năng nổi bật</h2>
      <p class="section-sub">Được thiết kế tỉ mỉ với những thành phần bạn cần để xuất bản nhanh chóng.</p>
      <div class="features">
        <article class="feature">
          <span class="tag">UI sạch</span>
          <h3>Thiết kế tối giản</h3>
          <p>Khoảng trắng hợp lý, hệ màu thương hiệu, font Inter đẹp mắt và dễ đọc.</p>
        </article>
        <article class="feature">
          <span class="tag">Responsive</span>
          <h3>Hiển thị mọi thiết bị ok hiện thị</h3>
          <p>Tự động co giãn trên di động, máy tính bảng và màn hình lớn.</p>
        </article>
        <article class="feature">
          <span class="tag">Dark/Light</span>
          <h3>Chuyển giao diện</h3>
          <p>Giao diện sáng/tối lưu lại lựa chọn, dịu mắt, chuyên nghiệp.</p>
        </article>
        <article class="feature">
          <span class="tag">Hiệu ứng</span>
          <h3>Chuyển động mượt</h3>
          <p>Shadow, glass, gradient và floating card giúp trang sống động.</p>
        </article>
        <article class="feature">
          <span class="tag">Form</span>
          <h3>Liên hệ nhanh</h3>
          <p>Form đẹp, dễ tích hợp dịch vụ gửi mail (Formspree, Zapier...).</p>
        </article>
        <article class="feature">
          <span class="tag">SEO</span>
          <h3>Chuẩn SEO cơ bản</h3>
          <p>Thẻ meta, heading, tốc độ tốt giúp cải thiện hiển thị tìm kiếm.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- ===== Showcase ===== -->
  <section id="du-an">
    <div class="container">
      <h2 class="section-title">Mẫu giao diện</h2>
      <p class="section-sub">Một vài khối nội dung gợi ý để trình bày sản phẩm/dịch vụ của bạn.</p>
      <div class="showcase">
        <div class="card">
          <img src="https://images.unsplash.com/photo-1522071820081-009f0129c71c?q=80&w=1600&auto=format&fit=crop" alt="Team làm việc" />
          <div class="meta">
            <strong>Bộ ảnh thương hiệu</strong>
            <div class="muted">Kể câu chuyện doanh nghiệp thật ấn tượng.</div>
          </div>
        </div>
        <div class="card">
          <img src="https://images.unsplash.com/photo-1515879218367-8466d910aaa4?q=80&w=1600&auto=format&fit=crop" alt="Laptop code" />
          <div class="meta">
            <strong>Giao diện sản phẩm</strong>
            <div class="muted">Ảnh chụp màn hình, mô tả rõ ràng & CTA nổi bật.</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== Pricing ===== -->
  <section id="bang-gia">
    <div class="container">
      <h2 class="section-title">Bảng giá tham khảo</h2>
      <p class="section-sub">Chọn gói phù hợp, có thể tùy chỉnh theo nhu cầu.</p>
      <div class="pricing">
        <div class="price-card">
          <div class="badge">Tiết kiệm</div>
          <h3>Basic</h3>
          <div class="price">$49</div>
          <div class="list">
            <div>1 trang Landing</div>
            <div>Responsive cơ bản</div>
            <div>Hỗ trợ qua email</div>
          </div>
          <a class="btn btn-ghost" href="#lien-he">Chọn gói này</a>
        </div>
        <div class="price-card">
          <div class="badge">Phổ biến</div>
          <h3>Pro</h3>
          <div class="price">$149</div>
          <div class="list">
            <div>3 trang (Home/Blog/Liên hệ)</div>
            <div>Dark mode + hiệu ứng</div>
            <div>Tối ưu tốc độ</div>
          </div>
          <a class="btn btn-primary" href="#lien-he">Chọn gói này</a>
        </div>
        <div class="price-card">
          <div class="badge">Doanh nghiệp</div>
          <h3>Business</h3>
          <div class="price">$399</div>
          <div class="list">
            <div>Tùy biến thương hiệu</div>
            <div>Tích hợp form/email</div>
            <div>Hỗ trợ ưu tiên</div>
          </div>
          <a class="btn btn-ghost" href="#lien-he">Chọn gói này</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== Contact ===== -->
  <section id="lien-he">
    <div class="container">
      <h2 class="section-title">Liên hệ</h2>
      <p class="section-sub">Điền thông tin, mình sẽ phản hồi sớm nhất.</p>
      <form onsubmit="event.preventDefault(); alert('Đã gửi! (Demo)'); this.reset();">
        <div class="grid" style="display:grid; grid-template-columns: 1fr 1fr; gap:12px;">
          <input required placeholder="Họ và tên" />
          <input required type="email" placeholder="Email" />
        </div>
        <textarea rows="5" placeholder="Nội dung cần tư vấn"></textarea>
        <button class="btn btn-primary" type="submit">Gửi yêu cầu</button>
      </form>
    </div>
  </section>

  <footer>
    <div class="container" style="display:flex; justify-content:space-between; gap:16px; flex-wrap:wrap;">
      <div>© <span id="y"></span> Trang Web Đẹp. All rights reserved.</div>
      <div>
        <a href="#">Điều khoản</a> · <a href="#">Chính hi aooooooooooooooassssách</a>
      </div>
    </div>
  </footer>

  <script>
    // Dark / Light theme with memory
    const themeToggle = document.getElementById('themeToggle');
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
    const saved = localStorage.getItem('theme');
    if(saved){ document.documentElement.setAttribute('data-theme', saved); }
    else{ document.documentElement.setAttribute('data-theme', prefersDark ? 'dark' : 'light'); }

    themeToggle.addEventListener('click', () => {
      const current = document.documentElement.getAttribute('data-theme');
      const next = current === 'dark' ? 'light' : 'dark';
      document.documentElement.setAttribute('data-theme', next);
      localStorage.setItem('theme', next);
    });

    // Mobile menu
    const menuBtn = document.querySelector('.menu-toggle');
    const menu = document.getElementById('menu');
    menuBtn.addEventListener('click', () => menu.classList.toggle('open'));

    // Year
    document.getElementById('y').textContent = new Date().getFullYear();

    // Reveal on scroll (simple)
    const revealEls = document.querySelectorAll('.feature, .price-card, .card');
    const io = new IntersectionObserver((entries)=>{
      entries.forEach(e=>{
        if(e.isIntersecting){
          e.target.style.transform = 'translateY(0)';
          e.target.style.opacity = '1';
          io.unobserve(e.target);
        }
      })
    }, { threshold: .12 });
    revealEls.forEach(el=>{
      el.style.transform = 'translateY(12px)';
      el.style.opacity = '.001';
      el.style.transition = 'transform .6s ease, opacity .6s ease';
      io.observe(el);
    });
  </script>
</body>
</html>
GitHub / development activity – Number of commits, pull‑requests, or new releases for a protocol’s codebase; a rise often precedes roadmap‑driven price moves.

GitHub / development activity – Number of commits, pull‑requests, or new releases for a protocol’s codebase; a rise often precedes roadmap‑driven price moves.


Specific API Endpoints: List the exact API endpoints used for placing orders, retrieving quotes, checking order status, and accessing market data. Version numbers of the APIs are essential, outlining the protocol's ability to adapt to API changes.
Data Formats: Precise definition of the data formats (e.g., JSON, Protobuf) used for all API calls and responses.
Authentication and Authorization: Detailed explanation of how the protocol authenticates and authorizes its API requests. This must include details about API keys, signing mechanisms (e.g.,
